{
  "name": "@b4m-oss/the-wheels-monorepo",
  "version": "0.10.4",
  "private": false,
  "workspaces": [
    "site",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/the-wheels",
    "dev": "npm run dev -w site",
    "build:site": "npm run build -w site",
    "dev:lib": "npm run dev -w packages/the-wheels"
  },
  "devDependencies": {
    "sass": "^1.83.4",
    "vituum": "^1.1.1",
    "vue-tsc": "^2.1.10"
  },
  "dependencies": {
    "@b4m-oss/the-wheels": "^0.8.0"
  }
}
